<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- ✅ IZIN BIOMETRIK: Diperlukan agar aplikasi bisa mengakses sensor sidik jari & Face ID -->
    <!-- Tanpa izin ini, local_auth.authenticate() akan melempar PlatformException -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />

    <!-- ✅ IZIN FINGERPRINT (Kompatibilitas Android < 9 / API level < 28) -->
    <uses-permission android:name="android.permission.USE_FINGERPRINT" />

    <!-- ✅ IZIN INTERNET (Tidak digunakan, namun dicantumkan sebagai best practice template) -->
    <!-- Hapus baris ini jika ingin aplikasi 100% offline -->
    <!-- <uses-permission android:name="android.permission.INTERNET" /> -->

    <application
        android:label="SYNC"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="false"
        android:fullBackupContent="false">

        <!-- 
            ✅ FLAG KEAMANAN: android:allowBackup="false"
            Mencegah data aplikasi ter-backup ke Google Drive / ADB backup.
            Ini KRUSIAL untuk aplikasi jurnal privat agar data enkripsi tidak bocor.
        -->

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize"
            android:showWhenLocked="false"
            android:turnScreenOn="false">

            <!-- 
                ✅ FLAG KEAMANAN: android:showWhenLocked="false"
                Mencegah aplikasi ditampilkan saat layar terkunci,
                ini memastikan konten jurnal tidak bisa dilihat tanpa membuka kunci.
            -->

            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>

    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>

</manifest>
